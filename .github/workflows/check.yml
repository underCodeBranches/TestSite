name: Check
on: push
jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v6
            - name: Install deps
              run: npm ci
            - name: Lint
              run: npm run lint
    test:
        needs: [lint]
        strategy:
          matrix:
            version: [14,16,18]
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v6
            - uses: actions/setup-node@v6
              with:
                node-version: 16
            - name: Install deps
              run: npm ci
            - name: Test
              run: npm run test